# 2026-08-09 빌드 플레이 테스트 버그·기능 9건 검증

## 결과

- PART A 완료: 목표/튜토리얼 내용과 `건너뛰기 (Y×2)` 영역의 화면 Rect가 겹치지 않음. 패널 Rect는 1080p에서 y=932~1056으로 안전 영역 안에 위치. Caps Lock 토글은 기존 목표 표시 상태를 재사용하며 0.25초 페이드/슬라이드 적용.
- PART B 완료: `Products/CatCatalog` 200종 전수 검사에서 아이콘 누락 0건, 3D 외형(VisualPrefab 또는 PrizePrefab) 누락 0건. 아이콘 조회 경로도 누락 0건.
- PART C 완료: 계산 확인 1번 선택 후 프롬프트가 즉시 닫히고 추가 E 입력 없이 판매 완료. PlayMode 실측에서 구매 0→1, 자금/판매 수 반영 확인. 2번 흥정 경로는 유지.
- PART D 완료: 경쟁 가게 사장 순찰점은 `(14.75,0.10,-18.50)`, `(27.75,0.10,-18.50)`, `(27.75,0.10,-11.50)`, `(14.75,0.10,-11.50)`. 이번 방문 도난품 1개를 개인 인벤토리에서만 회수해 경쟁점 재고로 반환했고 자금 변화는 0원.
- PART E 완료: 상단 자금 옆 알바 버튼 확인. 미고용 시 `알바 고용`, 고용 후 `배치 관리`로 바뀌며 기존 알바 관리 화면을 연다.
- PART F 완료: 창고 박스 재질 셰이더 `Universal Render Pipeline/Lit`, 색상 `RGBA(0.420,0.200,0.080,1.000)` 확인. 분홍 셰이더 오류 없음.
- PART G 완료: 공용 창고에서 상품 2000을 개인 인벤토리로 이동한 같은 이벤트 흐름에서 퀵슬롯이 `2000,-1,-1,-1,-1`로 갱신되고 1번 아이콘 texture/enabled가 모두 true.
- PART H 완료: 기계 선택 목록이 한국어 표시명 5개와 기본 업무로 표시됨. 2번 알바가 배치 후 `(7.20,0.05,-8.70)`에서 기계 앞 `(23.00,0.00,13.15)` 방향으로 이동해 `(20.69,0.03,12.79)`까지 접근(남은 거리 2.34m). 3번 알바 고용 완료/배치 행이 즉시 활성화됨.
- PART I 완료: 경찰 권위 오브젝트에 CharacterController와 장애물 회피 적용. 3.2m 폭 장애물 테스트에서 충돌 겹침 false, 측면 우회 0.49m, 보이는 방향과 실제 속도 방향 dot=1.000.

## 회귀 확인

- Unity 전체 스크립트 재컴파일: error 0.
- PlayMode 새 세션: ShopNetworkGame spawned, 플레이어 1명, HUD 정상.
- 런타임 주요 시스템: claw 3대, gacha 4대, kuji 2대, 판매/업그레이드/HUD/알바/절도 시스템 인스턴스 정상.
- 최종 PlayMode 콘솔 error 0.
- 이번 지시서 규칙에 따라 플레이어 빌드는 생성하지 않음.

## 증거

- `2026-08-09_tutorial_objective_final.png`: 1080p 목표/건너뛰기 UI 최종 배치.
- `2026-08-09_warehouse_material.png`: 창고 재고 외형의 URP 재질 확인.
- `2026-08-09_staff_assignment_final.png`: 한국어 기계명과 2·3번 알바 관리 화면 확인용 캡처.

## 참고

- 저장소에 기존 미커밋 변경이 광범위하게 겹쳐 있어, 사용자 변경을 함께 스테이징하지 않도록 이번 변경은 자동 커밋하지 않았다.
